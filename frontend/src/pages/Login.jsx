import { ArrowRight, Sparkles } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { useState } from "react";
import { useAuth } from "../context/AuthContext";
function Login() { const { login } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ email: "", password: "" }); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); const submit = async (event) => { event.preventDefault(); setLoading(true); setMessage(""); try { const response = await login({ ...form, email: form.email.trim() }); navigate(response.profile?.onboarding_completed ? "/dashboard" : "/onboarding", { replace: true }); } catch (error) { setMessage(!error.response ? "Cannot reach TinyPal. Make sure the Django server is running." : error.response.status === 401 ? "That email and password do not match an active account." : error.response.data?.detail || "Login could not be completed."); } finally { setLoading(false); } }; return <main className="min-h-screen bg-[#FAFAFC] p-5"><div className="mx-auto grid min-h-[calc(100vh-2.5rem)] max-w-5xl overflow-hidden rounded-3xl border border-indigo-100 bg-white shadow-[0_24px_80px_rgba(79,70,229,.12)] lg:grid-cols-2"><section className="flex items-center justify-center p-7 sm:p-12"><form onSubmit={submit} className="w-full max-w-sm"><Link to="/" className="text-lg font-semibold text-indigo-600">TinyPal</Link><h1 className="mt-10 text-3xl font-semibold text-gray-900">Welcome back</h1><p className="mt-2 text-gray-500">Pick up right where you left off.</p><label className="mt-8 block text-sm font-medium text-gray-700">Email<input className="mt-2 w-full rounded-xl border border-gray-200 px-3 py-3 outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" type="email" value={form.email} onChange={(event) => setForm({ ...form, email: event.target.value })} required /></label><label className="mt-5 block text-sm font-medium text-gray-700">Password<input className="mt-2 w-full rounded-xl border border-gray-200 px-3 py-3 outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" type="password" value={form.password} onChange={(event) => setForm({ ...form, password: event.target.value })} required /></label>{message && <p className="mt-4 text-sm text-red-600">{message}</p>}<button className="mt-7 flex w-full items-center justify-center gap-2 rounded-xl bg-indigo-500 py-3 font-semibold text-white hover:bg-indigo-600" disabled={loading}>{loading ? "Logging in..." : <>Log in <ArrowRight size={17} /></>}</button><p className="mt-7 text-sm text-gray-500">New here? <Link className="font-semibold text-indigo-600" to="/signup">Create an account</Link></p></form></section><aside className="relative hidden overflow-hidden bg-gradient-to-br from-indigo-600 to-violet-600 p-10 text-white lg:block"><div className="relative z-10"><div className="inline-flex items-center gap-2 rounded-full bg-white/15 px-3 py-1.5 text-sm"><Sparkles size={15} /> Plan with confidence</div><h2 className="mt-8 text-4xl font-semibold">A better study day starts here.</h2></div><img src="/images/tinypal-productivity-space.png" alt="Floating study and work tools" className="absolute bottom-0 right-0 w-[100%]" /></aside></div></main>; }
export default Login;
