import { ArrowRight, Sparkles } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { useState } from "react";
import { useAuth } from "../context/AuthContext";
function Signup() { const { register } = useAuth(); const navigate = useNavigate(); const [form, setForm] = useState({ full_name: "", email: "", password: "", confirm_password: "" }); const [message, setMessage] = useState(""); const [loading, setLoading] = useState(false); const submit = async (event) => { event.preventDefault(); setLoading(true); setMessage(""); try { await register(form); navigate("/login", { replace: true }); } catch (error) { const data = error.response?.data; setMessage(typeof data === "object" ? Object.values(data).flat().join(" ") : "Account could not be created."); } finally { setLoading(false); } }; const field = (label, name, type = "text") => <label className="mt-4 block text-sm font-medium text-gray-700">{label}<input className="mt-2 w-full rounded-xl border border-gray-200 px-3 py-3 outline-none focus:border-indigo-500 focus:ring-2 focus:ring-indigo-100" type={type} value={form[name]} onChange={(event) => setForm({ ...form, [name]: event.target.value })} required /></label>; return <main className="min-h-screen bg-[#FAFAFC] p-5"><div className="mx-auto grid min-h-[calc(100vh-2.5rem)] max-w-5xl overflow-hidden rounded-3xl border border-indigo-100 bg-white shadow-[0_24px_80px_rgba(79,70,229,.12)] lg:grid-cols-2"><aside className="relative hidden overflow-hidden bg-gradient-to-br from-violet-600 to-indigo-600 p-10 text-white lg:block"><div className="relative z-10"><div className="inline-flex items-center gap-2 rounded-full bg-white/15 px-3 py-1.5 text-sm"><Sparkles size={15} /> Your planning space</div><h1 className="mt-8 text-4xl font-semibold">Build a plan that fits your life.</h1></div><img src="/images/tinypal-productivity-space.png" alt="Floating study and work tools" className="absolute bottom-0 right-0 w-[100%]" /></aside><section className="flex items-center justify-center p-7 sm:p-10"><form onSubmit={submit} className="w-full max-w-sm"><Link to="/" className="text-lg font-semibold text-indigo-600">TinyPal</Link><h2 className="mt-7 text-3xl font-semibold text-gray-900">Create your account</h2>{field("Full name", "full_name")}{field("Email", "email", "email")}{field("Password", "password", "password")}{field("Confirm password", "confirm_password", "password")}{message && <p className="mt-4 text-sm text-red-600">{message}</p>}<button className="mt-7 flex w-full items-center justify-center gap-2 rounded-xl bg-indigo-500 py-3 font-semibold text-white hover:bg-indigo-600" disabled={loading}>{loading ? "Creating account..." : <>Create account <ArrowRight size={17} /></>}</button><p className="mt-6 text-sm text-gray-500">Already have an account? <Link className="font-semibold text-indigo-600" to="/login">Log in</Link></p></form></section></div></main>; }
export default Signup;
